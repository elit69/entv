<div class="form-wrapper product">
  <h2><%= @products[0].company %></h2>
  <ul>
    <% @products.each do |p| %>
      <li>
        <%= link_to(p.name, product_path(p)) %>
      </li>
    <% end %>
  </ul>
</div>